One method is added to the XML class (src/main/java/org/json).
It is:
    - toFutureJSONObject(Reader)
    
Search with "@author Mandy Tsai" to locate them quickly.

Two methods are added to the JSONObjectTest class (src/test/java/org/json/junit/data).
The test cases are the last two methods in the XMLTest class; the method names are:
    - toFutureJSONObject()
    - toFutureJSONObjectInvalidXML()
